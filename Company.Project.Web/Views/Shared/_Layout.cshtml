<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>SB Admin 2 - Bootstrap Admin Theme</title>
    
    <!-- base css -->
    @Styles.Render("~/bundles/bootstrap_css")
    @Styles.Render("~/bundles/animate_css")
    @Styles.Render("~/bundles/bootstrap-dropdownhover_css")
    
    <!-- module css -->
    @Styles.Render("~/bundles/morris_css")
    @Styles.Render("~/bundles/datatables_css")
    
    <!-- app css -->
    @Styles.Render("~/bundles/app_css")
    
    <!-- base js -->
    @Scripts.Render("~/bundles/modernizr")
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/bootstrap-dropdownhover")

    <!-- direct url -->
    <link href="@Url.Content("~/Vendor/font-awesome/css/font-awesome.min.css")" rel="stylesheet" type="text/css" />
</head>
<body>
    <div class="wrapper">
        <!-- Navigation -->
        @Html.Partial("_Navbar");
        <div id="page-wrapper" class="page-wrapper">
            @RenderBody()
        </div>
        <!-- /#page-wrapper -->
    </div>
    <div class="modal fade bs-logout-modal-sm" tabindex="-1" role="dialog" aria-hidden="true" aria-labelledby="logoutmodal" id="logoutmodal">
        <div class="modal-dialog modal-sm">
            <div class="modal-content" style="border-radius: 0px;">
                <div class="modal-header"><h4>Logout <i class="fa fa-lock"></i></h4></div>
                <div class="modal-body"><i class="fa fa-question-circle"></i> Are you sure you want to logout?</div>
                <div class="modal-footer">
                    @using (Html.BeginForm("Logoff", "Account"))
                    {
                        @Html.AntiForgeryToken()
                        <input type="submit" id="modallogoutbtn" value="Logoff" class="btn btn-primary" />
                    }
                </div>
            </div>
        </div>
    </div>
    <!-- /#wrapper -->
 
    <!-- module -->
    @Scripts.Render("~/bundles/raphael")
    @Scripts.Render("~/bundles/datatables")

    <!-- custom js -->
    @Scripts.Render("~/bundles/app")
    @RenderSection("scripts", required: false)
</body>
</html>

